# Production environment configuration for FireCalc UI
# Copy this file to modules/ui/.env.production and customize as needed

# Backend API Configuration
# For production, use your actual production domain and HTTPS
VITE_BACKEND_PROTOCOL=https
VITE_BACKEND_HOST=api.firecalc.afpma.pro
VITE_BACKEND_PORT=443
VITE_BACKEND_BASE_PATH=/v1
