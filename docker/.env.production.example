# =============================================================================
# FireCalc Docker Environment Variables - Production Overrides
# =============================================================================
# Copy to .env.production and fill in your actual values:
#   cp docker/.env.production.example docker/.env.production
#
# Only variables that differ from defaults.
# Merged on top of .env.defaults by setup-env.sh.
# =============================================================================

# Environment identity
UI_DOMAIN=firecalc.example.com
API_DOMAIN=api.example.com
COMPOSE_PROJECT_NAME=firecalc-production
FIRECALC_ENV=production
INVOICE_PREFIX="FCALC-[YYYY]-"
LETSENCRYPT_STAGING=false

# Email/SMTP credentials
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USERNAME="your-smtp-username"
SMTP_PASSWORD="your-smtp-password"
EMAIL_FROM_ADDRESS="noreply@example.com"
EMAIL_FROM_NAME="FireCalc"

# GoCardless credentials (LIVE)
GOCARDLESS_ACCESS_TOKEN="your_live_access_token_here"
GOCARDLESS_WEBHOOK_SECRET="your_webhook_secret_here"
GOCARDLESS_ENVIRONMENT=live
GOCARDLESS_BASE_URL="https://api.gocardless.com"
GOCARDLESS_REDIRECT_URI="https://${API_DOMAIN}/v1/payment_complete"
GOCARDLESS_EXIT_URI="https://${API_DOMAIN}/v1/payment_cancelled"

# JWT secret (generate with: openssl rand -base64 32)
JWT_SECRET="GENERATE_WITH_openssl_rand_-base64_32"

PRODUCT_CATALOG=production
REPORT_AS_DRAFT=false
